version: '3.8'
services:
  backend:
    build:
      context: ./Backend
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    volumes:
      - ./Backend/uploads:/app/uploads
    environment:
      - APP_UPLOADS_PATH=/app/uploads
    restart: unless-stopped
